---

---
<script>
  const updateThemeIcons = (isDark: boolean) => {
    const lightModeIcon = document.getElementById("lightModeIcon");
    const darkModeIcon = document.getElementById("darkModeIcon");

    if (lightModeIcon) lightModeIcon.hidden = !isDark;
    if (darkModeIcon) darkModeIcon.hidden = isDark;
    document.getElementById("themeToggle")?.setAttribute("aria-pressed", String(isDark));
  };

  const initThemeToggle = () => {
    const button = document.getElementById("themeToggle");
    if (!button || button.dataset.themeReady === "true") return;

    button.dataset.themeReady = "true";
    updateThemeIcons(document.documentElement.classList.contains("dark"));
    button.addEventListener("click", () => {
      const isDark = !document.documentElement.classList.contains("dark");
      document.documentElement.classList.toggle("dark", isDark);
      localStorage.setItem("theme", isDark ? "dark" : "light");
      updateThemeIcons(isDark);
    });
  };

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initThemeToggle, { once: true });
  } else {
    initThemeToggle();
  }
  document.addEventListener("astro:page-load", initThemeToggle);
</script>

<div class="flex flex-wrap xl:gap-4 justify-center items-center p-4 flex-none w-full bg-slate-500 dark:bg-slate-800">
		<a class="uppercase text-xs xl:text-2xl flex shrink-0 xl:gap-0.5 justify-center items-center font-extrabold hover:text-slate-800 dark:hover:text-slate-400 transition duration-300 ease-in-out" href="/">
			<h1 class="hidden md:block">
				Yousef El-Ghoul
			</h1>
			<h1 class="md:hidden">
				<span class="material-symbols-outlined">home</span>
			</h1>
		</a>
		<div class="grow">
		</div>
		<!-- <a class="header-link" href="/certifications">
			<h1>
				Certifications	
			</h1>
		</a>
		<h1 class="px-2">&#8226</h1> -->
		<a class="header-link" href="/projects">
			<h1>
				Projects	
			</h1>
		</a>
		<h1 class="px-2">&#8226</h1>
		<a class="header-link" href="/experience">
			<h1>
				Experience
			</h1>
		</a>
		<h1 class="px-2">&#8226</h1>
		<a class="header-link gap-1" href="https://resume.yousefelghoul.dev" target="_blank">
			<h1>
				Resume
			</h1>
			<span class="material-symbols-outlined !text-xs xl:!text-xl">open_in_new</span>
		</a>
		<button
			id="themeToggle"
			type="button"
			class="p-2.5 rounded-xl bg-slate-400 hover:bg-zinc-300 dark:bg-slate-700 dark:hover:bg-zinc-700 transition-colors flex items-center justify-center shadow-sm"
		>
		<span id="lightModeIcon" class="material-symbols-outlined text-xl" hidden>
			light_mode
		</span>
		<span id="darkModeIcon" class="material-symbols-outlined text-xl">
			dark_mode
		</span>
		</button>
</div>

<style>
	@reference "tailwindcss";

	.header-link {
		@apply uppercase text-xs xl:text-xl flex xl:gap-0.5 justify-center items-center hover:text-slate-800 dark:hover:text-slate-400 transition duration-300 ease-in-out
	}

	#lightModeIcon[hidden],
	#darkModeIcon[hidden] {
		display: none;
	}
</style>
